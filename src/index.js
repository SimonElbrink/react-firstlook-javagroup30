import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Needed for actually displaying the WebApplication in the HTML file.
ReactDOM.render(
    <App/>,
    document.getElementById('root')
);

